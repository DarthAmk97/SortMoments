# Core dependencies
PyQt6>=6.4.0
opencv-python>=4.7.0
numpy>=1.21.0
Pillow>=9.0.0
Pillow-Heif>=0.4.0

# Face detection and recognition (InsightFace unified pipeline)
insightface>=0.7.0

# ONNX Runtime with DirectML - works with ANY GPU (NVIDIA, AMD, Intel) on Windows
# No CUDA installation required!
# Windows (DirectML)
onnxruntime-directml>=1.15.0 ; platform_system == "Windows"

# macOS/Linux CPU fallback
onnxruntime>=1.15.0 ; platform_system != "Windows"

# Build dependencies (optional - only needed to create executable)
# pyinstaller>=5.0

